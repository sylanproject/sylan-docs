# On-chain vs Off-chain

**On-chain:** payments, identities, request IDs, finality, events.  
**Off-chain:** provider endpoints, node fetching, content hashing, transport.

Use IPFS or HTTPS for payload transport. Only **hashes** + **signatures** anchor on-chain.
