# SylanToken (SYL)

- ERC-20 compliant token used for payments and staking.
- Deployed with controlled minting and vesting integration.

**Common Ops**
- `approve(spender, amount)`
- `transfer(recipient, amount)`
- `balanceOf(holder)`
