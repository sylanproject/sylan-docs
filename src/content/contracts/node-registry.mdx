# NodeRegistry

**Responsibilities**
- Node registration, stake/unstake
- Reputation and (future) slashing hooks

**Core Functions**
- `registerNode(...)`
- `stake(amount)` / `unstake(...)`
- `setReputation(node, score)` (implementation-specific)
