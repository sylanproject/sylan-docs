# Deploy — Testnet

- Use Hardhat/Foundry with private key + RPC.
- Deploy order: Token → Logger → AccessRegistry → APIEscrow → APIConsensus → NodeRegistry → Staking/Vesting → Presale.
- Publish addresses to [/architecture/addresses-abis](/architecture/addresses-abis).
