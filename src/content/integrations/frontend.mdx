# Frontend Integration (Next.js + Wagmi + viem)

- Use Wagmi v2 and viem for contract calls.
- Keep ABIs in `app/lib/abis/*.json` and addresses via env.

**Example (approve + lock)** — see [Quickstart](/quickstart).
