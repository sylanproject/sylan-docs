# Errors & Reverts

Common:
- `bad requestId`: ensure AccessRegistry/Consensus compute the same ID and nonces match.
- `price=0`: plan not configured.
- `not PPC`: using `lockForCall` on a Subscription plan.

Fix wiring & inputs; compare against [Request Lifecycle](/concepts/request-lifecycle).
