# Node Setup

1. Clone `sylan-node` (coming soon).
2. Configure `.env` (RPC URL, private key, contract addresses).
3. Start the process; ensure logs show connection to **APIConsensus** and **AccessRegistry**.
